{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TaxEvent",
  "description": "Schema for blockchain events recorded by TaxGuard AI",
  "type": "object",
  "properties": {
    "eventId": {
      "type": "string",
      "description": "Unique ID for this event, e.g., UUID"
    },
    "eventType": {
      "type": "string",
      "enum": ["filing", "payment", "auditFlag", "adminChange"],
      "description": "Type of event"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when the event occurred"
    },
    "anonymizedUserId": {
      "type": "string",
      "description": "Anonymized identifier for taxpayer or admin"
    },
    "hashOfPayload": {
      "type": "string",
      "description": "Hash of the detailed off-chain data (SHA256 recommended)"
    },
    "notes": {
      "type": "string",
      "description": "Optional field for additional metadata",
      "maxLength": 500
    }
  },
  "required": ["eventId", "eventType", "timestamp", "anonymizedUserId", "hashOfPayload"]
}
